# Porta do servidor
PORT=

# URI de conexão com MongoDB Atlas (substitua pelos seus dados)
# Formato: mongodb+srv://<usuario>:<senha>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGO_URI=

# JWT Secret - Use uma string aleatória e segura em produção
JWT_SECRET=seu-secret-key-mude-em-producao

# Configurações SMTP para envio de emails (Nodemailer)
# Exemplo para Gmail:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu-email@gmail.com
SMTP_PASS=sua-senha-de-app
SMTP_FROM=seu-email@gmail.com

# Ambiente (development ou production)
NODE_ENV=development
